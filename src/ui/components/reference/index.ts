/**
 * Reference panel building blocks — the shared row + group used by the chat
 * codex, session lorebook, and novel codex panels to render one consolidated,
 * dense reference surface.
 */
export { ReferenceRow } from './ReferenceRow'
export { ReferenceGroup, ReferenceEmpty } from './ReferenceGroup'
