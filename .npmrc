# pnpm configuration (this project uses pnpm — see packageManager in package.json)

# Enforce the Node/pnpm versions declared in package.json "engines"
engine-strict=true

# Keep the strict, non-flat node_modules layout (pnpm default). Do not enable
# shamefully-hoist — code must declare every dependency it imports.
